# Projekt:Alpha — Python Core (Ubuntu Core-Stack)
fastapi>=0.115
uvicorn[standard]>=0.30
redis>=5.0
duckdb>=1.0
pyarrow>=16.0
numpy>=1.26
pandas>=2.2
httpx>=0.27
pydantic>=2.7
pyyaml>=6.0        # config/autonomy-level-4.yaml (Fallback: app/core/blueprint.py)
python-multipart>=0.0.9

# Security (WebAuthn) — optional, mit Fallback
webauthn>=2.0

# Loop C — vendored tradingview-scraper (vendor/tradingview-scraper)
requests>=2.32
beautifulsoup4>=4.12
websockets>=13.1
websocket-client>=1.8
python-dotenv>=1.0

# Ingestion (echte CCXT-Feeds) — optional, [MOCK-SEAM] Fallback aktiv
# ccxt>=4.3

# Test
pytest>=8.0
pytest-asyncio>=0.23
fakeredis>=2.23
lupa>=2.0
